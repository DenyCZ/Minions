# Configuration for ${project.name}
#
# Version: ${project.version}

# Debug information
debug-enabled: true

plugin-prefix: '&3Minions &8>> '

# Name of the folder that contains templates
template-folder: 'templates'